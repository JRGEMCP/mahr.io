import { MilestoneSectionModel } from './milestone-section-model';

describe('MilestoneSectionModel', () => {
  it('should create an instance', () => {
    expect(new MilestoneSectionModel('', '')).toBeTruthy();
  });
});
