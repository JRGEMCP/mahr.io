import { SubmitDesignFilter } from './submit-design.filter';

describe('SubmitDesignFilter', () => {
  it('should create an instance', () => {
    expect(new SubmitDesignFilter()).toBeTruthy();
  });
});
