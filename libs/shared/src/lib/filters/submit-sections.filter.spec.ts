import { SubmitSectionsFilter } from './submit-sections.filter';

describe('SubmitSectionsFilter', () => {
  it('should create an instance', () => {
    expect(new SubmitSectionsFilter()).toBeTruthy();
  });
});
